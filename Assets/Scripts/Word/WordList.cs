using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Set word set and get word
public class WordList : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
